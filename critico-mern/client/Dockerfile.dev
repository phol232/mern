# Dockerfile para desarrollo del Frontend (React + Vite) con HOT-RELOAD

FROM node:18-alpine

WORKDIR /app

# Instalar dependencias del sistema para mejor rendimiento
RUN apk add --no-cache \
    python3 \
    make \
    g++

# Copiar package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias
RUN npm install

# Exponer el puerto de Vite
EXPOSE 5173

# Comando para desarrollo con hot-reload optimizado
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
